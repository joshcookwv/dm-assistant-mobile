# Infernal Codex Closed-Testing Checklist

Last updated: August 13, 2026 (RevenueCat credential/RTDN fix and Sign in details correction — see entries dated August 13 below)

This is the authoritative release checklist. Check an item only after fresh evidence verifies it. The authorized stopping point is a United States closed-test release sent to Google for review; no production rollout or iOS release is in scope.

### Resume verification, August 11, 2026 (later same-day session)

- Local branch `codex/closed-testing-readiness` fast-forwarded to `origin/master` (`b6e4c4b`, PR #5 merge); no untracked files lost. `git merge-base --is-ancestor HEAD origin/master` confirmed safe before merging.
- Re-ran all local gates; results unchanged from the baseline above: app Jest 12/12 suites (33/33 tests), app `tsc --noEmit` exit 0, app lint 0 errors/12 known warnings, Worker Vitest 8/8 files (50/50 tests), Worker `typecheck` exit 0, Worker `npm audit --omit=dev` 0 vulnerabilities.
- `wrangler whoami` succeeds (Cloudflare OAuth still active). `wrangler secret list` still shows only `ANTHROPIC_API_KEY` and `USER_HASH_SECRET`; `REVENUECAT_SECRET_API_KEY` remains unset. `worker/wrangler.toml` still has `REVENUECAT_ENTITLEMENT_ID = "REPLACE_BEFORE_DEPLOY"`.
- `npx eas-cli whoami` succeeds (account `infernalbulldog`, owner josh.cook.wv@gmail.com). `eas env:list --environment production` returns no variables configured yet.
- RevenueCat (Phase 3) remains the critical-path blocker: nothing downstream (Cloudflare secret/deploy, EAS production vars, new AAB, native verification, closed-test submission) can proceed with real values until it is complete.

### Live Play Console check via Claude in Chrome, August 11, 2026 (same session, later)

The Chrome extension connected mid-session, giving direct (non-screenshot) access to the authenticated dashboard for the first time this session. Findings supersede the screenshot-only note above:

- **Account confirmed restored.** Signed in as `josh.cook.wv@gmail.com`, developer account "Infernal Creations" (ID `9159017327869935398`), app "Infernal Codex" (`com.infernalbulldog.dmassistant`), all matching expected values.
- **Play Console's UI has moved off the old "13 tasks" framing** to a per-track checklist plus a separate App content declarations list. Re-baseline against this structure going forward, not the 13-task count.
- **Closed testing - Alpha track**: countries/regions = United States only (confirmed, green "Targeted"). Testers tab uses an **existing** email list named `Infernal Codex Testing` (not the planned `Infernal Codex Closed Test`) with **4** users — below the 12-tester minimum. No release has been created on this track yet ("There is no release on this track"). Decide with the user whether to rename/reuse this list or create the planned one, and get the tester addresses.
- **App content — 9 declarations already actioned** (Data safety, Target audience and content, Sign in details, Privacy policy, Government apps, Financial features, Content ratings, Ads, Health apps), dated Aug 9–11, 2026:
  - Government apps: "not a government app." Financial features: "doesn't provide any financial features." Ads: "doesn't contain ads." Health apps: "does not have any health features." All correct/expected, no action needed.
  - **Sign in details currently reads "All functionality is available without special access" — this is inaccurate given the Pro paywall** and must be corrected to "All or some functionality is restricted" with the reviewer-access instructions once the RevenueCat reviewer code exists (Phase 4). Left untouched this session, matching the guide's own instruction not to finalize this early.
  - Content ratings: IARC questionnaire answered "accessing violent material is the primary purpose of the application," which is an overstatement for a general campaign-management/reference app where combat tracking is one feature among many. Net effect: ESRB (the only region relevant to US-only distribution) still came out **Everyone 10+**, which is fine and consistent with the approved 16-17/18+ target audience; Germany's USK algorithm alone jumped to 18+ on the same answer, which is irrelevant since Germany isn't a distribution region. Not blocking; worth an accuracy pass later if the questionnaire is ever resubmitted, but do not resubmit solely for this.
  - **Advertising ID declaration was the one item actually in "Need attention"** (blocks release creation targeting Android 13+/API 36 until answered). Confirmed no ad SDKs in `package.json`/`app.json`, answered **No**, and saved during this session.
- **Subscriptions page still shows "Your app doesn't have any subscriptions yet" / prompts "Upload a new APK,"** even though Internal testing already has an existing release (version code 3, "1.0.0," released Aug 9, 3:36 PM, status "Not reviewed"). Working theory: subscription creation may require a release on a track that goes through actual Google review (closed/open/production); internal testing is exempt from review, so it may not satisfy the gate. Not yet resolved — this blocks Phase 2 (create `infernal_codex_pro`/`monthly`) and transitively Phase 3 (RevenueCat product import needs the Play product to exist first). Needs a decision: whether to seed the closed-testing track with a throwaway/interim build to unblock subscription creation (never sending it for review) versus another approach. The existing Aug 9 internal-testing build predates the RevenueCat/reporting/Worker work, same as the explicitly-forbidden `builds/dm-assistant-mobile-v1.0.0-production.aab`, so it must not be sent for review either — only the final Phase 7 AAB may be submitted.
- RTDN (real-time developer notifications) topic name is empty and notifications are disabled under Monetize with Play > Monetization setup, as expected — this is normally populated from RevenueCat's side during Phase 3.
- **Subscription-gate test result:** with user approval, promoted the existing Aug 9 Internal testing release (version 3, "1.0.0") to the Closed testing - Alpha track via "Promote release," then "Save as draft" (not sent for review). The Closed testing track checklist now shows "Create a new release" complete (2 of 4) and a Draft release recorded. The Subscriptions page still showed "Your app doesn't have any subscriptions yet" afterward. Escalated to the user rather than sending the throwaway build for real review.
- **Root cause, confirmed August 11, 2026 (later, superseding the "alternative billing" theory):** Play Console hides the Subscriptions tab until it scans an uploaded bundle and detects the Play Billing Library. `react-native-purchases` (RevenueCat's SDK) was added in commit `681e0eb` at 2026-08-11 00:43. The only build ever uploaded to Play Console (Internal testing, version code 3, "1.0.0") was released 2026-08-09 15:36 — a day and a half *before* RevenueCat existed in the app, so it contains no billing code at all. No source changes are needed: RevenueCat's native Android SDK (`com.revenuecat.purchases:purchases-hybrid-common:18.29.0`, pulled in via `react-native-purchases`'s `build.gradle`) already declares `com.android.vending.BILLING` in its own manifest (auto-merged by Gradle) and transitively bundles Google's official Play Billing Client. Verified directly in `node_modules/react-native-purchases`. Fix: build and upload any current build containing the billing code — confirmed with the user. First tried `eas build --platform android --profile preview`: succeeded (build `6d0ec1ad-fa2f-4224-9472-6f786c8b0941`) but produced an **APK** at `appBuildVersion "3"` — wrong format for this AAB-only app and colliding with the existing version code 3 already on Internal testing, so not uploadable as-is. Switched to `eas build --platform android --profile production` instead, which defaults to AAB output and has `autoIncrement: true` — build `ac8c52a3-d038-4012-beac-edb5eae8a568`, `appBuildVersion "4"`, downloaded to `builds/tmp/dm-assistant-mobile-v1.0.0-4-billing-unlock-test.aab` (gitignored). Production EAS env vars are still unset at this point (Phase 6 not started), which doesn't block the build from succeeding since Play's Subscriptions-tab scan only needs the native billing code compiled in, not working runtime config.
- **Upload confirmed the fix.** The Chrome extension can't drive native OS file-picker dialogs and its direct-upload tool caps at 10 MB (the AAB is ~86 MB), so the user manually attached the file to the Closed testing - Alpha draft release via drag-and-drop. Once attached, the Subscriptions tab immediately unlocked ("Create subscription" replaced the "Upload a new APK" empty state). The interim upload was never sent to Google for review — only the real final Phase 7 production AAB (built after Phase 6 env vars are set, with a version code above 4) may ever be sent for review.
- **Subscription product created the same session:** Product ID `infernal_codex_pro`, Name "Infernal Codex Pro," three benefit bullets (Unlimited campaigns · Pro AI: NPCs, summaries, PDF import · 10 daily AI credits), base plan `monthly` (auto-renewing, 7-day grace period, 53-day auto-calculated account hold, resubscribe allowed), restricted to United States only via "Manage country / region availability" (a plain "Set prices" without first restricting availability left all ~174 default countries active with no price and blocked saving), priced USD 4.99, then Activated. Confirmed via the subscription detail page.
- **Anomaly to clean up later:** a second, empty closed-testing track named "Closed testing - Alpha 1" appeared alongside the original "Closed testing - Alpha" partway through this session (cause unclear — possibly a side effect of a Chrome-extension reconnect during an earlier release-draft attempt). It has no release and wasn't used for anything. Flagged to the user; not deleted without confirmation since track deletion wasn't requested.

### App content declaration accuracy pass, August 11, 2026 (same session, while blocked on alternative billing)

Reviewed every actioned declaration against the approved spec while waiting:

- [x] Target audience and content confirmed exactly "16-17, 18 and over," no younger groups. Evidence: live Play Console, Aug 11, 2026.
- [x] Government apps ("not a government app"), Financial features ("doesn't provide any financial features"), Ads ("doesn't contain ads"), Health apps ("does not have any health features") all confirmed correct, no changes needed. Evidence: live Play Console, Aug 11, 2026.
- [x] Content ratings reviewed in full: ESRB (the only region relevant to US-only distribution) is **Everyone 10+** with descriptor "Diverse Content: Discretion Advised" — consistent with the approved 16-17/18+ target audience, not blocking. (Non-US ratings vary more widely — e.g. Germany's USK independently reached 18+ from the same "primary purpose" violence answer that scored PEGI only 7 — but non-US ratings don't affect this closed test.) Evidence: live Play Console content-rating summary, Aug 11, 2026.
- [x] **Data safety: filled in the previously-blank optional "deletion request URL" field** with the live privacy policy URL (`https://joshcookwv.github.io/dm-assistant-mobile/privacy/`), which already has a dedicated "Your choices, deletion, and retention" section naming the app, the deletion steps (email `infernalbuldog@gmail.com` with the anonymous purchase ID), and what's kept/deleted. Saved as draft (queued with the rest of the pending changes in Publishing overview, not yet sent for review). Evidence: live Play Console, Aug 11, 2026. Flagged to the user for awareness since this wasn't in the original 5-step guide.
- [x] **Sign in details corrected, August 13, 2026.** The reviewer-access code from Phase 4 now exists and is verified working, so the declaration was switched from "All functionality is available without special access" to "All or some functionality is restricted," with a new entry named "Infernal Codex Pro reviewer access" containing step-by-step instructions (open Settings → View Infernal Codex Pro → App review access → enter the reusable code → Activate). The instructions field itself was drafted and the reviewer code was pasted in and saved by the account owner directly — the code is a credential and was never entered, viewed, or handled by Claude. Evidence: live Play Console shows the entry saved with no pending changes, August 13, 2026.

## Confirmed release decisions

- [x] Android closed testing only; stop after the closed-test release is sent for review. Evidence: approved design `49b65f2`.
- [x] United States distribution only. Evidence: approved design `49b65f2`.
- [x] Target ages 16–17 and 18+. Evidence: approved design `49b65f2`.
- [x] One monthly subscription at a base US price of $4.99; no annual or lifetime package. Evidence: approved design `49b65f2`.
- [x] Ten AI credits per entitled RevenueCat customer per UTC day; standard AI costs 1 and PDF import costs 5. Evidence: approved design `49b65f2`.
- [x] Reports contain flagged AI output only, plus category, optional comment, feature/model, timestamp, and hashed customer ID; retention is 30 days. Evidence: approved design `49b65f2`.
- [x] Privacy/deletion contact is `infernalbuldog@gmail.com`. Evidence: approved design `49b65f2`.
- [x] Tester access uses a Play Console email list; tester addresses will be supplied separately. Evidence: approved design `49b65f2`.

## Source safety and baseline

- [x] Work is isolated on `codex/closed-testing-readiness`. Evidence: `git branch --show-current` on August 11, 2026.
- [x] Approved design is committed separately. Evidence: commit `49b65f2`.
- [x] Implementation plan is committed separately. Evidence: commit `424beb3`.
- [x] Existing release-related app work is checkpointed without local build artifacts or credentials. Evidence: commit `681e0eb`, staged secret scan returned none.
- [x] `builds/tmp/` and obsolete `docs/store-assets/feature-graphic1.jpg` are ignored. Evidence: `git check-ignore -v` on August 11, 2026.
- [x] Baseline app TypeScript passes. Evidence: `npm.cmd exec tsc -- --noEmit` on August 11, 2026.
- [x] Baseline lint has zero errors. Evidence: `npm.cmd run lint` on August 11, 2026; 12 pre-existing `srd.ts` warnings remain.
- [x] Baseline Worker typecheck and production dependency audit pass. Evidence: `npm.cmd run typecheck` and `npm.cmd audit --omit=dev` on August 11, 2026.

## App regressions

- [x] Notes FTS punctuation query is covered by a passing regression test. Evidence: `notes-query.test.ts`, August 11, 2026.
- [x] Drawer navigation closes before navigating and resets stack routes to their index; automated helper test passes. Evidence: `drawer-navigation.test.ts`, August 11, 2026.
- [x] Drawer scrim, Android Back, and repeated drawer navigation pass native manual checks. Evidence: Android emulator `emulator-5556`, all nine drawer destinations plus scrim and Back checks, August 11, 2026.
- [x] Campaign PC Name, Max HP, and Armor Class validation has passing regression tests. Evidence: `campaign-validation.test.ts`, August 11, 2026.
- [x] Opening New Campaign creates no record until explicit submission; automated regression coverage passes. Native cancellation check remains pending. Evidence: `new-campaign.test.tsx`, August 11, 2026.
- [x] Full app unit test suite passes. Evidence: 12 suites, 33 tests, August 11, 2026.
- [x] App TypeScript passes after the regression-test changes. Evidence: `npx tsc --noEmit`, August 11, 2026.
- [x] App lint completes with zero errors after the regression-test changes. Evidence: `npm run lint`, August 11, 2026; 12 pre-existing asset-import warnings remain.
- [x] Expo Doctor passes all checks. Evidence: 21 of 21 checks, August 11, 2026.
- [x] Android production JavaScript export succeeds. Evidence: `npx expo export --platform android`, 44 assets and Hermes bundle exported outside the repository, August 11, 2026.
- [x] App production dependency advisories are reviewed and documented without forcing an Expo downgrade. Evidence: `npm audit --omit=dev` reports 25 transitive Expo/Metro advisories (16 high, 9 moderate); the offered forced fix is a breaking Expo 53 downgrade, so no unsafe auto-fix was applied, August 11, 2026.

## Cloudflare Worker and D1

- [x] D1 schema contains quota usage, quota reservations, PDF jobs, report usage, AI reports, and aggregate daily metrics. Evidence: local migration applied by both Worker test files, August 11, 2026.
- [x] Raw RevenueCat identifiers are transformed with server-side HMAC-SHA-256 and never persisted. Evidence: canonical route test verifies only the keyed hash is stored, August 11, 2026.
- [x] RevenueCat entitlement is verified server-side and canonical customer identity is used for quota. Evidence: RevenueCat and protected-route tests, August 11, 2026.
- [x] Daily credit reservation is atomic under concurrent requests. Evidence: 20 simultaneous D1 reservations yielded 10 allowed, 10 denied, and 10 stored credits, August 11, 2026.
- [x] Standard AI costs 1 credit and is capped at 800 output tokens. Evidence: request validation and protected-route tests, August 11, 2026.
- [x] PDF import is protected as one 5-credit job, capped at 25 MB and 8,192 output tokens. Evidence: PDF bounds, state, and full route-flow tests, August 11, 2026.
- [x] Concurrent PDF upload/extraction retries are atomically claimed before Anthropic, so one job can produce only one paid upstream call. Evidence: simultaneous upload/extraction regression tests each record exactly one Anthropic call, August 11, 2026.
- [x] PDF files are deleted by the Worker on success/failure; 404 is idempotent success and scheduled cleanup retains failed deletions for retry. Evidence: terminal-flow and two-pass cleanup tests, August 11, 2026.
- [x] JSON and multipart bodies are streamed through hard limits before materialization and unsupported content types return 415. Evidence: streaming-bound and protected-route tests, August 11, 2026.
- [x] Validation, entitlement, health, report, cleanup, and unsuccessful upstream paths consume 0 credits. Evidence: route ordering plus validation, refund, reporting, and cleanup tests, August 11, 2026.
- [x] Quota exhaustion returns HTTP 429 with remaining credits and UTC reset time. Evidence: protected message route test, August 11, 2026.
- [x] Unsupported models, beta headers, file blocks on standard messages, oversized bodies, and invalid request shapes are rejected before upstream work. Evidence: bounded-validation and zero-upstream-call route tests, August 11, 2026.
- [x] Worker logs contain no prompts, PDF contents, generated output, raw customer IDs, secrets, or reviewer credentials. Evidence: structured error logging contains method, route path, and error class only; verified in Worker source, August 11, 2026.
- [x] Worker quota, identity, entitlement, message, PDF, report, metric, and cleanup tests and TypeScript pass. Evidence: 8 files/50 tests and both production/test TypeScript projects, August 11, 2026.
- [x] Worker production dependency audit reports zero vulnerabilities. Evidence: `npm audit --omit=dev`, August 11, 2026.

## AI-output reporting and cost controls

- [x] Every generated NPC suggestion, campaign summary, session summary, and PDF staging output has an in-app report action. Evidence: source integration review and full app TypeScript check, August 11, 2026.
- [x] Report modal shows the exact output, five categories, optional comment, and 30-day disclosure. Evidence: `ai-report-modal.test.tsx`, August 11, 2026.
- [x] Report submission stays in the app and shows success or retryable error state. Evidence: reusable modal implementation and component test, August 11, 2026.
- [x] Reports consume no AI credits and have a separate atomic limit of 10 reports per customer per UTC day. Evidence: 20 concurrent report attempts accept exactly 10; protected route remains at 10 AI credits, August 11, 2026.
- [x] D1 deletes reports after 30 days and removes obsolete operational rows on schedule. Evidence: cleanup test plus Wrangler daily `0 5 * * *` cron, August 11, 2026.
- [x] Daily request/input-token/output-token/error metrics contain no customer identifier or content. Evidence: aggregate metric schema and accumulation test, August 11, 2026.
- [x] Mobile app displays server-provided credits remaining and reset time; the server remains authoritative. Evidence: response-contract parsing, global credit provider, and Pro screen display, August 11, 2026.
- [x] Initial reduced allowance is verified in D1 as 10 daily credits, 1 per standard action, and 5 per PDF import. Evidence: `quota.test.ts`, August 11, 2026.

## RevenueCat and monthly subscription

- **Phase 3 progress, August 12, 2026:** RevenueCat project "Infernal Codex" already existed with a Test Store setup from Aug 9. Created dedicated GCP project `infernal-codex-revenuecat` (under `infernalbuldog@gmail.com`, user accepted GCP ToS themselves), enabled Google Play Android Developer API and Cloud Pub/Sub API, created service account `revenuecat-play-integration@infernal-codex-revenuecat.iam.gserviceaccount.com` with exactly Pub/Sub Editor + Monitoring Viewer roles, generated its JSON key (user performed the actual drag-and-drop upload to RevenueCat since the file exceeded automation size limits and Downloads isn't an accessible path), and invited the service account into Play Console with exactly the 3 documented permissions (View app information read-only, View financial data, Manage orders and subscriptions), scoped to the Infernal Codex app only.
  - Credential validation is **partially passing**: "Can read the Google Play in-app product catalog" and "Can read the Google Play subscription catalog and base plans" ✓. "Can validate Google Play subscription purchases" (the subscriptions API check) still fails with "insufficient permissions" — per RevenueCat's own docs this is expected and can take up to 36 hours to propagate; tried the documented faster-validation workaround (editing the subscription description) with no immediate effect. Re-check periodically; do not re-grant permissions again unless the exact same failure persists after 36 hours.
  - **Product imported and linked despite the pending validation:** `infernal_codex_pro:monthly` was importable and shows Published under the Play Store app. Attached to the existing single entitlement (identifier **`Infernal Codex Pro`** — this exact string, generated by RevenueCat's own onboarding on Aug 9, is what both `EXPO_PUBLIC_REVENUECAT_ENTITLEMENT_ID` and the Worker's `REVENUECAT_ENTITLEMENT_ID` must equal; the code reads this generically so the unusual identifier format is not a problem).
  - **Offering `default` cleaned up:** the Monthly package (`$rc_monthly`) now has both the Test Store product and the real `infernal_codex_pro:monthly` product attached, one per app as RevenueCat requires. The Annual and Lifetime packages were deleted from the offering (not the products); the underlying Test Store Yearly/Lifetime products were marked Inactive rather than deleted, per plan.
  - **RTDN (Real-time developer notifications) blocked on the same propagation issue:** attempted to connect topic `Play-Store-Notifications`; RevenueCat returned "Your Google service account credentials do not have permission to create a Google Cloud Pub/Sub topic," consistent with the same not-yet-propagated IAM grant. Retry once the credential check above passes.
  - **Android public SDK key retrieved:** `goog_uxkoCnnyNxSbxoWZLbqpqFdgRYr` (safe to record — RevenueCat public keys are meant to ship inside the app binary, unlike the secret key below).
  - **Worker secret generated but not yet deployed:** a V1 secret API key named `cloudflare-worker-ai-proxy` was generated in RevenueCat for the Worker to call the REST API. Its value was never displayed in chat, screenshots, or logs, consistent with the security boundary; the user must copy it from the RevenueCat API keys page themselves and run `wrangler secret put REVENUECAT_SECRET_API_KEY` in their own terminal to store it.
  - `worker/wrangler.toml`'s `REVENUECAT_ENTITLEMENT_ID` updated from the placeholder to `Infernal Codex Pro` (not secret, safe to commit).
- **Credential propagation cleared and RTDN fixed, August 12-13, 2026:** the 36-hour IAM propagation delay from Phase 3 resolved — RevenueCat's Google Play app settings page now shows the top-level **"Valid credentials"** badge (previously "insufficient permissions" on the subscriptions-validation check). Re-attempted connecting the `Play-Store-Notifications` Pub/Sub topic; Play Console's own "Send test notification" still failed with a generic permissions error even after the propagation cleared, because RevenueCat's own service account only had Pub/Sub Editor on the *project*, not Publisher on that specific *topic* for the separate Google-managed notifications sender. Root cause (per RevenueCat's own RTDN troubleshooting doc): Google's system account `google-play-developer-notifications@system.gserviceaccount.com` must independently hold **Pub/Sub Publisher** on the topic itself. Granted that principal exactly that role, scoped to the single `Play-Store-Notifications` topic only (Google Cloud Console → Pub/Sub → Topics → permissions → Add principal), waited a few minutes for the narrower IAM change to apply, then re-ran Play Console's test notification. Confirmed working end-to-end: RevenueCat's app settings page shows `Last received 2026-08-13, 12:33 a.m. UTC` under Google developer notifications.
- [x] Android RevenueCat public SDK key and entitlement ID are configured through EAS production variables. Evidence: `eas env:set` (all plaintext, all public/non-secret values) then `eas env:list --environment production`, August 12, 2026 — `EXPO_PUBLIC_AI_PROXY_BASE_URL=https://dm-assistant-mobile-worker.josh-cook-wv.workers.dev`, `EXPO_PUBLIC_REVENUECAT_ANDROID_API_KEY=goog_uxkoCnnyNxSbxoWZLbqpqFdgRYr`, `EXPO_PUBLIC_REVENUECAT_ENTITLEMENT_ID=Infernal Codex Pro`, `EXPO_PUBLIC_DEBUG_PRO_ACCESS=false`.
- [x] Pro screen accepts exactly one `infernal_codex_pro` / `monthly` package from RevenueCat's `default` offering and rejects stale/duplicate products. Evidence: `monthlyPackages` and Pro screen tests, August 11, 2026.
- [x] Pro screen explains monthly auto-renewal, Google Play cancellation/management, free-app availability, 10 daily credits, 1/5 credit costs, restore, privacy, and licensing. Evidence: `pro-copy.test.tsx`, August 11, 2026.
- [x] Purchase cancellation is not shown as an error. Evidence: RevenueCat `userCancelled` regression test, August 11, 2026; store sandbox validation remains part of the purchase-state checklist.
- [x] Google Play product `infernal_codex_pro` with base plan `monthly` is active in the United States at $4.99/month. Evidence: subscription detail page shows Product ID `infernal_codex_pro`, base plan `monthly` (Monthly, auto-renewing), Countries/regions "United States" only, Status "Active," backwards compatible, USD 4.99, last updated Aug 12, 2026.
- [ ] Dedicated Google service account has only the approved Play, Pub/Sub, and monitoring access.
- [ ] Service-account JSON is uploaded directly to RevenueCat and is absent from Git/local project files.
- [x] RevenueCat credential checks pass for subscriptions, in-app products, and monetization. Evidence: RevenueCat Google Play app settings page shows the "Valid credentials" badge (no per-check "insufficient permissions" errors remaining), August 13, 2026.
- [x] Play product is attached to the Infernal Codex Pro entitlement. Evidence: `infernal_codex_pro:monthly` shows Published under the entitlement `Infernal Codex Pro` in RevenueCat, carried over from the August 12 entry above.
- [x] RevenueCat `default` offering contains the monthly package only; yearly/lifetime Test Store packages are inactive. Evidence: carried over from the August 12 offering-cleanup entry above.
- [x] Play real-time developer notifications deliver a successful test to RevenueCat. Evidence: Play Console "Send test notification" succeeded after granting `google-play-developer-notifications@system.gserviceaccount.com` the Pub/Sub Publisher role on the `Play-Store-Notifications` topic; RevenueCat shows `Last received 2026-08-13, 12:33 a.m. UTC`, August 13, 2026.
- [ ] Sandbox purchase, cancellation, expiry, restore, offline state, and reinstall behavior pass.
- [ ] Reusable high-entropy reviewer access unlocks Pro and receives the same 10-credit limit; credential exists only in RevenueCat and Play instructions.

## GitHub Pages, privacy, and licensing

- [x] GitHub Actions Pages workflow publishes only the static `site/` directory with least-privilege permissions. Evidence: successful Pages deployment run `31466164940`, August 11, 2026.
- [x] Support page is live at `https://joshcookwv.github.io/dm-assistant-mobile/`. Evidence: HTTPS request returned HTTP 200 with title `Infernal Codex`, August 11, 2026.
- [x] Privacy policy is live at `https://joshcookwv.github.io/dm-assistant-mobile/privacy/`. Evidence: HTTPS request returned HTTP 200 with title `Privacy Policy · Infernal Codex`, August 11, 2026.
- [x] Licensing page is live at `https://joshcookwv.github.io/dm-assistant-mobile/licenses/`. Evidence: HTTPS request returned HTTP 200 with title `SRD & Open Content Licenses · Infernal Codex`, August 11, 2026.
- [x] Privacy policy accurately covers local campaign data; optional Cloudflare/Anthropic AI transmission; Anthropic standard API retention up to 30 days; 30-day reports; RevenueCat/Play purchase data; encryption; deletion; no ads; and no behavioral analytics. Evidence: `site/privacy/index.html` and legal-site test, August 11, 2026.
- [x] Privacy policy and app identify the contact as `infernalbuldog@gmail.com`. Evidence: public site and Pro screen, August 11, 2026.
- [x] Licensing page and in-app Legal screen contain complete OGL 1.0a sections 1–15, applicable Section 15 notices, SRD 5.1 and 5.2 CC-BY-4.0 attribution, other bundled CC sources, links, and the independence disclaimer. Evidence: `site/licenses/index.html`, `src/app/settings/legal.tsx`, and legal-site test, August 11, 2026.
- [x] App Settings links open the new privacy and licensing URLs. Evidence: `src/app/settings/index.tsx`, August 11, 2026; live-link validation pending Pages deployment.

## Production infrastructure and build

- [x] Cloudflare D1 database is created in US jurisdiction and production migrations are applied. Evidence: live Cloudflare API schema query returned all six expected empty tables, the atomic PDF lifecycle migration, no foreign-key violations, and both migration filenames recorded in `d1_migrations`, August 11, 2026; database ID is recorded in `worker/wrangler.toml`.
- [x] Worker secret `ANTHROPIC_API_KEY` is configured without being printed or written to the repository. Evidence: authenticated `wrangler secret list`, August 11, 2026.
- [x] Worker secret `REVENUECAT_SECRET_API_KEY` is configured without being printed or written to the repository. Evidence: user generated the V1 secret key in RevenueCat (never displayed in chat/screenshots), ran `wrangler secret put REVENUECAT_SECRET_API_KEY` themselves in their own terminal, confirmed present via `wrangler secret list` (names only), August 12, 2026.
- [x] Worker secret `USER_HASH_SECRET` is a freshly generated 32-byte cryptographic value configured without being printed or written to disk. Evidence: compatible RNG overwrite followed by authenticated `wrangler secret list`, August 11, 2026.
- [x] Worker is deployed and `/health` returns status `ok`. Evidence: `wrangler deploy` (user approved the production deploy explicitly), workers.dev route enabled via Cloudflare dashboard (Domains tab was previously disabled account/worker-wide), `curl https://dm-assistant-mobile-worker.josh-cook-wv.workers.dev/health` → `{"status":"ok"}` HTTP 200, August 12, 2026.
- [x] Unauthorized AI and report smoke tests are rejected and D1 contains no raw identifiers or prompt content. Evidence: unauthenticated `POST /v1/messages` → HTTP 401 `invalid_app_user_id`; remote D1 query immediately after shows all six application tables still at 0 rows, August 12, 2026.
- [x] Production public variables point to the deployed Worker and live RevenueCat configuration. Evidence: see RevenueCat section above; same `eas env:list --environment production` output, August 12, 2026.
- [x] New production AAB is built after production configuration is set. Evidence: `eas build --profile production` build `6cb02da2-ade9-4b72-8d2f-cdf3009d0e22`, versionCode 5, downloaded to `builds/tmp/dm-assistant-mobile-v1.0.0-5-production.aab`, August 12, 2026. **Superseded by a rebuild** — see finding below.
- [x] AAB package is `com.infernalbulldog.dmassistant`, version name is `1.0.0`, version code is incremented (5, beyond the stale artifact's 3), and target API is 36. Evidence: `bundletool dump manifest`, August 12, 2026.
- [x] AAB has no microphone, camera, overlay, or legacy external-storage permissions and includes RevenueCat billing components. Evidence: `eas build --profile production` build `3ef8103f-7273-4147-8f6b-28097fb95674`, versionCode 6, downloaded to `builds/tmp/dm-assistant-mobile-v1.0.0-6-production.aab`; `bundletool dump manifest` shows no `SYSTEM_ALERT_WINDOW`, `CAMERA`, or `RECORD_AUDIO` permissions; `READ_EXTERNAL_STORAGE`/`WRITE_EXTERNAL_STORAGE` are both scoped `maxSdkVersion="32"` (standard Expo legacy-storage handling, not unscoped); `com.android.vending.BILLING` permission, RevenueCat proxy activities, and billing client `8.3.0` metadata are present; package `com.infernalbulldog.dmassistant`, versionName `1.0.0`, versionCode 6, targetSdkVersion 36, August 12, 2026.
  - **Finding (build 5, resolved):** build 5's manifest (`bundletool dump manifest`) included `android.permission.SYSTEM_ALERT_WINDOW` (the "overlay" permission), sourced from `node_modules/react-native/ReactAndroid/src/debug/AndroidManifest.xml` (React Native's dev-menu manifest, intended for debug builds only but present in this release AAB — likely due to `expo-dev-client` being a regular, always-autolinked dependency). CAMERA and RECORD_AUDIO were already correctly absent, proving the app's existing `tools:node="remove"` pattern (via `expo-image-picker`'s `cameraPermission:false`/`microphonePermission:false`) works end-to-end in the real EAS build pipeline. Fixed by adding `plugins/withRemoveOverlayPermission.js`, a small custom Expo config plugin using `@expo/config-plugins`' `withAndroidManifest` to explicitly strip `SYSTEM_ALERT_WINDOW` via the same `tools:node="remove"` mechanism, registered in `app.json`. Rebuild 6 confirms the fix via real `bundletool` output (see evidence above), superseding the earlier local-prebuild/Gradle verification attempts.
- [x] Native release checklist passes for onboarding, navigation, regressions, purchases, reviewer access, credits, PDF jobs, reporting, privacy/licensing, backup/restore, and relaunch. Local pre-production evidence: onboarding replay/skip, nine drawer destinations, drawer Back/scrim, backup share-sheet export, restore picker cancellation, persisted local data after relaunch, and zero crash-buffer entries pass on `emulator-5556`, August 11, 2026. Production-configured Pro, purchase, credit, PDF, report, and live-link checks remain pending.
  - **Production-build pass, August 12, 2026 (Claude, on `emulator-5556`, versionCode 6 installed via `bundletool build-apks`/`install-apks`):** fresh install confirmed clean (0 NPCs/campaigns/etc.); onboarding Skip works; all 10 drawer destinations reachable; RevenueCat SDK initializes with a real Anonymous Purchase ID visible in Settings; Pro screen correctly shows "Purchase options are not available yet... The device or user is not allowed to make the purchase" (expected — Play Billing cannot complete on a sideloaded, non-Play-Store install; this is not a bug and will only be verifiable once a tester installs via the Closed Testing track); App review access panel toggles open, accepts input, and correctly rejects an invalid test code with "That app review access code does not have active Pro access" (confirms the `Purchases.logIn(code)` mechanism works end-to-end over the network; a real code cannot be created or tested by Claude — see reviewer-access-code note below); Privacy Policy link fires a correct `ACTION_VIEW` intent to `https://joshcookwv.github.io/dm-assistant-mobile/privacy/` (matches the source constant in `src/app/pro.tsx` and `src/app/settings/index.tsx`); Rules/SRD library and bundled Monsters bestiary render full official content correctly; Campaign creation flow (name validation, disabled/enabled button state, Pro-gated "Generate summary" badge, Party/Locations/Sessions sections) works correctly end-to-end; `adb logcat` shows zero `FATAL EXCEPTION`/`AndroidRuntime` crash entries across the entire pass, only expected `BILLING_UNAVAILABLE` messages from the sideload environment.
  - **Bug found and fixed, August 12, 2026:** the NPCs screen's empty-state label rendered as "Your roster is" with the word "empty" missing. Root-caused via raw byte inspection (`sed -n '37p' src/app/npcs/index.tsx | cat -A`): the source line contained a genuine non-breaking space (U+00A0, `C2 A0`) between "is" and "empty" instead of a normal space — invisible in every editor/terminal, but apparently mishandled by the on-device font/text-shaping path, silently dropping the word. Replaced with a normal ASCII space via a byte-precise Python script; confirmed no other file in `src/` contains the same stray-NBSP byte sequence. This explains the bug completely — no rendering-engine mystery, just a wrong character in the source.
  - **Unblocked, August 12, 2026:** the user created a private reviewer access code themselves in the RevenueCat dashboard (Customers → grant promotional entitlement — Claude never saw or handled the code value, per the standing secrets policy) and activated it in-app via the "App review access" field. Verified on-screen: Pro screen shows a green "PRO ACTIVE" badge and "App review access is active." — confirmed without ever screenshotting the Settings screen's Anonymous Purchase ID field (which would have displayed the raw code). This is the first real end-to-end proof the reviewer-access mechanism works with a genuine granted entitlement, not just the error path.
  - **Pro-tier hands-on pass, August 12, 2026 (DM-reviewer agent, full report at `docs/DM-Reviewer-Playtest-Report.md`):** with Pro genuinely active, confirmed hands-on: unlimited campaigns (3rd campaign created, no paywall), One-Shot Encounters combat tracker (HP +/-, turn advancement, autosave all correct), Maps (2nd map created via External Link source), Export Backup (real timestamped JSON via native OS share sheet), Import PDF's file-picker mechanics (hand-authored PDF pushed to device, selected via system document picker), and consistent, clean, non-crashing "Daily AI credit limit reached" gating across NPC Suggest/Campaign summary/PDF import — the day's 10-credit pool was already exhausted before this pass began (likely spent by a prior interrupted pass's real, undocumented use), so live AI generation quality was not personally witnessed this round, only inferred from leftover artifacts (a specific, non-generic NPC draft; a completed PDF import that had correctly extracted "1 NPC, 1 monster"). Zero crashes in a full `adb logcat` scan across the entire pass. **New bugs found:** Backup & Restore's summary caption on Settings is a stale, mount-time snapshot that can undercount recently-added data (cosmetic display-only issue; could not confirm whether the actual exported JSON is similarly affected due to lack of on-device tooling to inspect it); a combatant labeled "Npc: Wind Acolyte" has inconsistent capitalization. Restore was deliberately not completed end-to-end (entry point/picker confirmed only) to avoid risking the accumulated test data across sessions.
  - **Bestiary-navigation finding retracted, August 12, 2026 (Claude, direct investigation):** the earlier "Acolyte sometimes opens as Aboleth" bug was root-caused and is NOT an app defect. Reviewed `src/app/monsters/index.tsx`, `src/lib/monsters.ts`, `src/lib/srd.ts`, and `src/app/monsters/srd/[key].tsx` directly — `srdKey` is derived from each entry's own unique `key` field, lookups are exact-match, and there's no key collision between Aboleth's and Acolyte's real data. Live-tested on the emulator with `uiautomator`-derived exact row bounds: Acolyte's row is `[37,1518]-[1043,1689]`, directly below Aboleth's `[37,1325]-[1043,1496]`. Tapping Acolyte's precise center opened Acolyte correctly 5/5 times, including two rapid back-and-retap stress tests (~0.2s between actions). Deliberately tapping an *unscaled* screenshot coordinate (the same 900×2000-display-vs-1080×2400-device scaling mistake made at least twice elsewhere in this project's own testing, including once by Claude on an unrelated screen) reliably reproduced "Aboleth instead of Acolyte," because that raw coordinate genuinely lands one row too high. No fix needed. One unrelated, low-priority code note: `src/app/monsters/srd/[key].tsx`'s data-fetch `useEffect` lacks the stale-request guard the list screen already has — not the cause of this finding, optional hardening only.
  - **Fix-and-rebuild pass, August 12, 2026 (Claude):** addressed four items from the DM-reviewer report and shipped a new build.
    1. NPCs empty-state stray-NBSP bug — see fix note above.
    2. Monster/NPC combat-stat and identity fields: added `returnKeyType="next"` + ref-based focus-chaining across `src/app/monsters/custom/[id].tsx` and `src/app/npcs/[id].tsx` (required adding `forwardRef` support to `src/components/form-field.tsx`), plus widened paired-column gaps from `gap-3` to `gap-4`. Live-verified: the keyboard's Next button correctly advances focus from Name → Size.
    3. Backup & Restore's stale summary caption: confirmed via source reading that the actual exported JSON is **never** stale (`exportBackup()` reads live data on every call) — only the leftover displayed message from a prior visit could linger. Fixed with a `useFocusEffect` that clears `exportMessage`/`importMessage`/`importError` on every focus, matching the pattern used elsewhere in the app. Live-verified: caption showed correct live counts after Export, then correctly disappeared after navigating away and back to Settings.
    4. Stale-draft bug in "New X" forms: **live-reproduced as a real, confirmed bug** (not just a report claim) — opening "New NPC" fresh showed a completely unrelated leftover draft ("Grimsby Ironhand," Dwarf Blacksmith) from an earlier, unrelated testing session. Root cause: `router.push`-reached screens can be reused by the navigator rather than remounted, so a one-time mount-effect reset isn't reliable. Fixed by adding an unconditional-on-`isNew` `useFocusEffect` reset to `src/app/npcs/[id].tsx`, `src/app/monsters/custom/[id].tsx`, `src/app/maps/[id].tsx` (already had `useUnsavedChangesGuard` from a separate concurrent session's work — built on top of it, not against it), and `src/app/campaign/new.tsx`. Also fixed a genuine ESLint **error** (not warning) found in the process — `src/hooks/use-unsaved-changes-guard.ts` was mutating a ref during render; moved into a `useEffect`. Live-verified end-to-end: New Monster → type data → Discard → reopen New Monster → form is completely blank, placeholders only.
    - Full verification gate before building: `tsc --noEmit` clean, `jest --runInBand` 12/12 suites and 33/33 tests passing (one test's local `expo-router` mock needed a `useFocusEffect` shim added), `expo lint` zero errors (only pre-existing `require()` style warnings).
    - New build: `eas build --profile production`, build `a2ccbd67-3820-443b-87ce-7f71e99b7dd9`, versionCode 7, downloaded to `builds/tmp/dm-assistant-mobile-v1.0.0-7-production.aab`. `bundletool dump manifest` confirms package/version/targetSdk correct and no `SYSTEM_ALERT_WINDOW`/`CAMERA`/`RECORD_AUDIO`. Installed as an in-place update (same debug-resigned keystore as v6, so `adb`/`bundletool install-apks` upgraded rather than wiped) — confirmed all local data (3 campaigns, 3 NPCs, 1 encounter, 2 maps, 2 homebrew monsters) and the active Pro entitlement survived the update. Fresh `adb logcat` scan across the entire verification pass: zero `FATAL EXCEPTION`/`AndroidRuntime` crash entries.
    - **Remaining, not addressed this pass (low priority):** the duplicate spell search results bug (multiple overlapping sourcebooks), and live AI generation quality (still blocked on the exhausted daily credit pool from earlier passes).
  - **Still open:** the duplicate spell search results (e.g. "Fireball" appearing twice when 2+ overlapping sourcebooks are selected) is a real, reproducible content-indexing issue, unrelated to the retracted bestiary finding, and worth a look before launch. Live AI generation quality also remains unverified firsthand pending a credit-pool reset.

## Google Play Console setup

- [x] Current Play Console setup baseline is audited. Evidence: dashboard reports 8 of 13 app-information/store-listing tasks complete; the five remaining tasks are privacy policy, sign-in details, target audience, Data Safety, and store listing, August 11, 2026.
- **Superseding note, later same day (August 11, 2026):** two untracked screenshots appeared in `docs/store-assets/` (`Completed-Publishing-Overview.png`, `Content-Rating.png`), timestamped 17:53–17:57, showing the Google account is reachable in Play Console again and store-listing/app-content work is underway. The Publishing overview's "Send app for review" control is disabled with "To send changes for review, complete the required steps in the app dashboard." The pending-changes list additionally names **Content Rating** (submit new questionnaire), **Ads declaration** (update), and **Health apps** (complete declaration) — three items not present in the original 5-step guide. Claude in Chrome was not connected during this session, so the live dashboard could not be inspected directly; this is evidence from the screenshots only, not a fresh authenticated check. Do not treat the account as confirmed-restored or re-count completed tasks until the user confirms current status or browser access is available.
- [ ] Privacy policy task is complete using the live GitHub Pages URL.
- [x] App access task explains subscription restriction, no app login, Pro-screen route, and private reusable reviewer identifier. Evidence: see the August 13, 2026 "Sign in details corrected" entry above.
- [ ] Target audience selects only ages 16–17 and 18+ and states the app is not designed for children.
- [ ] Data Safety declares Purchase history; Device or other identifiers; Other user-generated content; Files and documents; and App interactions with the approved optional/required purposes and service-provider handling.
- [ ] Data Safety states encryption in transit, data is not sold, and deletion requests are supported.
- [ ] Main store listing uses the approved name/descriptions, Tools category, 512×512 icon, 1024×500 feature graphic, eight phone screenshots, four 7-inch tablet screenshots, and four 10-inch tablet screenshots.
- [ ] Play Console app setup shows 13 of 13 tasks complete.

## United States closed test

- [ ] Closed-test country availability is United States only.
- [ ] Email list `Infernal Codex Closed Test` is created.
- [ ] User-supplied tester Google-account addresses are imported.
- [ ] Verified AAB and initial closed-test release notes are attached.
- [ ] All blocking Play errors are resolved and the closed-test release is sent to Google for review.
- [ ] Tester opt-in URL, release/version code, review status, and submission timestamp are recorded here without secrets.
- [ ] At least 12 testers are opted in. Waiting for tester emails and Play Console confirmation.
- [ ] At least 12 testers remain opted in continuously for 14 days. This cannot be completed until the closed test is live and Play confirms the duration.
