{
  "name": "dm-assistant-ai-proxy",
  "version": "0.1.0",
  "private": true,
  "description": "Bundled-AI backend for Premium subscribers — see README.md before deploying.",
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^26.2.0",
    "typescript": "~6.0.3"
  }
}
