{
  "name": "dm-assistant-mobile-worker",
  "version": "1.0.0",
  "description": "Cloudflare Worker AI proxy for Infernal Codex Pro — verifies RevenueCat access, rate-limits per client, and forwards approved AI requests.",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit && tsc --project test/tsconfig.json --noEmit",
    "types": "wrangler types --env-file .dev.vars.example"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.21.0",
    "typescript": "^7.0.2",
    "vitest": "^4.1.10",
    "wrangler": "^4.120.0"
  }
}
